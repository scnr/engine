<% if issue.platform_type %>
    <p class="alert alert-info">
        Affected platform type belongs to the
        <strong><%= SCNR::Engine::Platform::Manager::TYPES[issue.platform_type] %></strong>
        category, identified as
        <strong><%= SCNR::Engine::Platform::Manager.new.fullname issue.platform_name %></strong>.
    </p>
<% end %>
